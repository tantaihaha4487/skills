# Repository guidance

Keep each skill self-contained under `skills/` with a required `SKILL.md`. Preserve bundled `references/`, `scripts/`, and `agents/openai.yaml` files when updating a skill. Validate changed skills with the Codex skill creator validator before committing.

